{
  "name": "screencap",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc --noEmit && vite build",
    "test:integration": "tsc --project tsconfig.integration.json && node .integration-tests/tests/run-integration-tests.js",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "tauri:dev": "node scripts/tauri-cli.cjs dev",
    "tauri:build": "node scripts/tauri-build.cjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-dialog": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "typescript": "^4.9.5",
    "vite": "^2.9.18"
  }
}
